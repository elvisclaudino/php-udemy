<?php

  // Definição do contador
  $x = 0;

  // Definição da estrutura
  while($x < 10) {

    // Corpo do loop
    echo $x . "<br>";

    // Incremento do contador
    $x++;
  }

  echo "Continuando o código... <br>";

  // Definição do contador
  $y = 0;

  // Definição da estrutura
  while($y <= 10) {

    // Corpo do loop
    echo $y . "<br>";

    // Incremento do contador
    $y += 2;
  }

  echo "Continuando o código... <br>";

  // Definição do contador
  $z = 10;

  // Definição da estrutura
  while($z > 0) {

    // Corpo do loop
    echo $z . "<br>";

    // Incremento do contador
    $z--;
  }

  echo "Continuando o código... <br>";

  // Definição do contador
  $a = 10;

  // Definição da estrutura
  while($a > 0) {

    // Corpo do loop
    if ($a % 2 != 0) {
      echo $a . "<br>";
    }
    
    // Incremento do contador
    $a--;
  }

  echo "Continuando o código... <br>"

?>